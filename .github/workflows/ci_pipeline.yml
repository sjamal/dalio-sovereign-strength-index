name: Continuous Integration & Verification Pipeline

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  validate_codebase:
    runs-on: ubuntu-latest
    steps:
      # Native Linux environment check setup bypassing legacy Node wrapper loops
      - name: Checkout Code Repository Natively
        uses: actions/checkout@v4
        with:
          sparse-checkout: |
            .gitignore
            requirements.txt
            src
            tests
            scripts
            docs

      - name: Deploy Native Python 3.11 Environment Runtime
        run: |
          sudo apt-get update
          sudo apt-get install -y python3.11 python3.11-venv python3-pip

      - name: Install System Dependencies
        run: |
          python3.11 -m pip install --upgrade pip
          python3.11 -m pip install -r requirements.txt
          python3.11 -m pip install pytest

      - name: Verify Core Ingestion Pass
        run: |
          export PYTHONPATH=src
          python3.11 src/run_brain.py

      - name: Run Test Verification Suite via Pytest
        run: |
          export PYTHONPATH=src
          python3.11 -m pytest tests/
